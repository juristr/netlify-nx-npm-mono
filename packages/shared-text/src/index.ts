export const getGreeting = (name: string): string => {
  return `Hello ${name}! Welcome to our awesome monorepo.`;
};

export const getCardText = (title: string): string => {
  return `This is a beautiful card component with title: ${title}`;
};

export const getButtonLabel = (action: string): string => {
  return `Click me to ${action}!`;
};
